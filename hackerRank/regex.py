import re

# Valid PAN format
"""
tests = int(raw_input())
regex = "^([A-Z]{5})(\d{4})([A-Z])$"
for test in xrange(tests):
	pan = raw_input()
	if re.search(regex, pan):
		print "YES"
	else:
		print "NO"
"""

# Valid ID number
"""
t = int(raw_input())
regex = "^([a-z]{0,3})(\d{2,8})([A-Z]{3,})$"
for x in xrange(t):
        id = raw_input()
        if re.search(regex, id):
                print "VALID"   
        else:
                print "INVALID" 
"""
